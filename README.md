# Latex圖文貼

## 今天的工作重點

1. 建立並同步 `handout` 專案到 GitHub 遠端。
2. 將產品名稱調整為 `Latex圖文貼`。
3. 調整題目輸入流程，區分為：
   - 幾何題使用 `貼上題圖`
   - 文字題使用 `貼上題目`
4. 加入文字題的 LaTeX / Markdown 渲染能力。
5. 新增上方 `字體大小` 欄位，讓文字題字級可調整。
6. 調整文字題排版規則：
   - 文字向上對齊
   - 超出框線時自動縮小
   - 目標是不超出框線
7. 修正題圖顯示方式，讓圖片更貼近填滿題目框。
8. 修正列印樣式與列印預覽版面，避免頁面裁切與比例異常。
9. 將目前修改推送到 GitHub `main` 分支。
